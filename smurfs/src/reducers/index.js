
//set initial state
const initialState = {
    smurfs: null,
    error: "",
    loading: false
};


//create reducer
export const reducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
};